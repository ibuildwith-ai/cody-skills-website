# Version Tasklist – v1.1.0
This document outlines all the tasks to deliver this particular version, grouped by phases.

| Status |      |
|--------|------|
| 🔴 | Not Started |
| 🟡 | In Progress |
| 🟢 | Completed |


## Phase 1 — Data Flip & Scaffolding
_Convert `cody-article-writer/skill.ts` from `ComingSoonSkill` to `AvailableSkill`, scaffold the 20 content stubs, and confirm the shell auto-handles the new skill without code changes. By the end of this phase, every sidebar link for `/docs/cody-article-writer/...` resolves to a placeholder page and the marketing landing already shows CAW as "available."_

| ID  | Task             | Description                             | Dependencies | Status | Assigned To |
|-----|------------------|-----------------------------------------|--------------|--------|-------------|
| 1.1 | Flip `skill.ts` to `AvailableSkill` | Rewrite `src/skills/cody-article-writer/skill.ts`. Change import + interface from `ComingSoonSkill` to `AvailableSkill`. Set `status: 'available'`, `version: '3.0'`, `github: 'https://github.com/ibuildwith-ai/cody-article-writer'`, `getSkill[]` with real hrefs to `/skills/cody-article-writer/downloads/cody-article-writer.{zip,skill}`, and the full 19-page `sidebar` tree per `design.md` §3. Keep existing `tagline` and `illustration` unchanged. **Done. Status, version (3.0), github URL, getSkill (2 entries), and the 4-group/19-item sidebar all populated.** | None | 🟢 Completed | AGENT |
| 1.2 | Verify build succeeds after flip | Run `npm run build`. Confirm no TypeScript errors from the union narrowing (CAW now appears in `availableSkills`). Confirm the marketing-landing card automatically renders as "available" in the build output. **Done. Build green in 967ms; 17 pages produced; no TS errors; no CAW routes yet (expected — content stubs come in task 1.5).** | 1.1 | 🟢 Completed | AGENT |
| 1.2a | Build Sidebar override component | Resolves v1.0.0 known debt: `astro.config.mjs` line 25 derives sidebar from `defaultSkill` only, so `/docs/cody-article-writer/...` would show CPB's sidebar. Build `src/components/Sidebar.astro` that reads `Astro.url.pathname`, uses the existing `getActiveSkill()` helper to find the active skill, and renders that skill's `sidebar` tree. **Done. `src/components/Sidebar.astro` reuses Starlight's internal `SidebarPersister` + `SidebarSublist` so DOM/CSS match exactly. Builds the entry list structurally (matches the runtime shape SidebarSublist reads) and casts the internal `SidebarEntry` type — noted in a comment for future Starlight upgrades.** | 1.1 | 🟢 Completed | AGENT |
| 1.2b | Wire Sidebar override + drop single-skill derivation | In `astro.config.mjs`: add `Sidebar: './src/components/Sidebar.astro'` to the `components` map. **Done. Added `Sidebar: './src/components/Sidebar.astro'` to `components`. Left the existing static `sidebar` derivation in place — Starlight may use it for routing helpers and the override ignores it anyway.** | 1.2a | 🟢 Completed | AGENT |
| 1.3 | Verify per-route sidebar swap | Build + dev. Navigate to `/docs/cody-product-builder/` → sidebar shows CPB's 4 groups (Getting Started, Workflow, Commands, Reference). Navigate to `/docs/cody-article-writer/` → sidebar swaps to CAW's 4 groups (Getting Started, Workflow, Writing Styles, Reference, 19 items total). **Done. Grepped rendered HTML: CAW page contains "Writing Styles", "Editor Pass & Export", "Style Guides Overview", "Triggers & Commands" in sidebar; CPB page contains ":cody plan/build/prototype/idea/refresh/help", "Plan Phase", "Prototypes". Confirmed sidebars are distinct and source from each skill's `skill.ts`.** | 1.2b | 🟢 Completed | AGENT |
| 1.4 | Resolve Mermaid support question | Try a Mermaid `flowchart` code block in one stub page. If Starlight 0.37.6 renders it natively, use Mermaid for "The Article Workflow" diagram (task 2.4). If not, document the ASCII fallback approach in this row and proceed with ASCII art for 2.4. **Done. Probed with a test `mermaid` code fence; Starlight rendered it as a plain `<pre data-language="mermaid">` code block (no diagram). Per design.md decision, no new dependencies. Task 2.4 uses ASCII boxes-and-arrows (mirrors v1.0.0 design.md architecture diagram style).** | 1.2 | 🟢 Completed | AGENT |
| 1.5 | Create content stubs (Overview + 19 sidebar pages) | Create `src/skills/cody-article-writer/index.md` plus the 19 sub-pages under `getting-started/`, `workflow/`, `writing-styles/`, `reference/`. Each stub has frontmatter `title:` + `description:` matching the sidebar label, and a one-line body: `"Placeholder. Real content arrives in Phase 2."`. Page filenames must match the slugs in `skill.ts`. **Done. All 20 files created with frontmatter title + description. Descriptions are real one-liners that double as SEO meta descriptions, not throwaway text.** | 1.1 | 🟢 Completed | AGENT |
| 1.6 | Verify all 20 URLs return 200 | `npm run build`, then walk every CAW URL: `/docs/cody-article-writer/`, `/docs/cody-article-writer/getting-started/installation/`, all six workflow pages, all six writing-styles pages, all four reference pages. Each should render the stub. No 404s, no broken sidebar links. **Done. Build produced 36 pages total (17 CPB + landing/404/docs-redirect + 20 CAW = 36). All 20 CAW URLs present in build output; no errors.** | 1.5 | 🟢 Completed | AGENT |


## Phase 2 — Content Drafting
_Replace each stub with real content drafted from the real skill source at `~/.claude/skills/cody-article-writer/` and from the Cody Article Writer FigJam. User reviews everything together in Phase 4. Page → source mapping is in `design.md` §3._

| ID   | Task             | Description                             | Dependencies | Status | Assigned To |
|------|------------------|-----------------------------------------|--------------|--------|-------------|
| 2.1  | Overview (`index.md`) | Skill landing. Frontmatter title + description (drives SEO). Hook, "Why it exists", capability summary, two-major-features framing (article workflow + style guide system), where-to-start CTA. Drafted from `SKILL.md` intro and Command Reference. Do NOT paraphrase the trigger-detection frontmatter verbatim — see `design.md` Open Question. **Done. Hook + Why it exists + What it helps you do + How it works (article workflow + style guides) + Triggers table + Where to start. Mirrors CPB Overview structure. No trigger-detection prose lifted verbatim.** | 1.6 | 🟢 Completed | AGENT |
| 2.2  | Installation | How to add CAW to the user's environment. Same shape as CPB's Installation page: two install paths (`.skill` vs skill folder), per-agent path table, activation notes, "what gets created" file tree (`cody-projects/article-writer/{styles,drafts,articles,archive}` per `SKILL.md` Directory Setup). **Done. Two install options + per-agent path table + activation via plain-language phrases (not `:cody` commands) + folder structure under `cody-projects/article-writer/` + Next CTA. Mirrors CPB Installation page structure.** | 1.6 | 🟢 Completed | AGENT |
| 2.3  | Quick Start | End-to-end first article: trigger the skill ("write an article about X"), walk through topic ideation, skip comprehensive research, pick or create a style, generate title/thesis/outline, write section-by-section, skip editor pass, export. Mirrors CPB's 5-step Quick Start structure. **Done. 8-step walkthrough: start an article → research depth decision → style → title/thesis/outline → write → approve/editor → export → keep going. Cross-links to deeper pages at each step.** | 1.6 | 🟢 Completed | AGENT |
| 2.4  | The Article Workflow | High-level visual tour of the 12-phase workflow. Embed the workflow diagram (Mermaid per task 1.4, otherwise ASCII art). Above the diagram: one-paragraph framing of "iterative loops at every phase + research integration at 6 points." Below: brief callouts to each phase's deeper page. Drafted from `SKILL.md` Article Workflow Overview + the FigJam. **Done. ASCII flowchart of all 12 phases with iteration loops + research integration points + decision diamonds (matches FigJam logic). Then "four key ideas" (iteration, style drives writing, research integrated, drafts preserve everything) and a per-phase table linking to deeper pages.** | 1.6, 1.4 | 🟢 Completed | AGENT |
| 2.5  | Topic Ideation & Research | Combined page covering Phases 1–2 of the article workflow plus research integration points 1–3. Sections: Topic Ideation (with always-on exploratory research), Research Planning Decision (yes/no), Comprehensive Research (light/medium/heavy depths, source approval, citation configuration). Drafted from `references/article-workflow.md` Phases 1–2 + `references/research-workflow.md` Points 1–3. **Done. Phase 1 (ideation + always-on exploratory) → Phase 2 (yes/no branch + depth/approval/citation) with draft state snippets and a table of where research shows up later in the workflow.** | 1.6 | 🟢 Completed | AGENT |
| 2.6  | Style Selection | Phase 3 — selecting an existing style for this article, or creating a new one inline. Cross-link to the Writing Styles category for full style guide details. Drafted from `references/article-workflow.md` Phase 3 + `references/style-workflow.md` recommendation offer. **Done. Branches on "have saved styles" vs not; covers guided vs starter creation; table of what gets applied at each later phase; "styles reusable, research is not" clarification.** | 1.6 | 🟢 Completed | AGENT |
| 2.7  | Title, Thesis & Outline | Phases 4–6 — title/thesis development (with research integration point 4), outline generation (with point 5), section confirmation. Drafted from `references/article-workflow.md` Phases 4–6 + `references/research-workflow.md` Points 4–5. **Done. Three sub-sections (Phase 4 + 5 + 6) with the AI radiology worked example for required-source integration, iteration loops at each phase, what gets saved, and the "rolls forward" summary before writing starts.** | 1.6 | 🟢 Completed | AGENT |
| 2.8  | Writing the Article | Phase 7 — section-by-section vs full-draft mode comparison, the "Write Article" iteration loop, inline citation format `[^1]` (research integration point 6), how formatting settings get applied. Drafted from `references/article-workflow.md` Phase 7 + `references/research-workflow.md` Point 6. **Done. Mode A vs Mode B comparison table + per-mode flow + citation insertion mechanics + working draft file explanation (.md vs .json) + what's saved.** | 1.6 | 🟢 Completed | AGENT |
| 2.9  | Editor Pass & Export | Phases 8–12 — the article-approval loop, the editor-decision diamond, the optional editor pass (loop), metadata generation, citation choice at export, final export to `articles/`, archive of the draft JSON. Drafted from `references/article-workflow.md` Phases 8–12. High-level only — the editor pass ruleset lives in Reference/Editor Style Guide. **Done. All five phases with the editor pass calibrated/always-applied split (cross-link to Editor Style Guide ref page), the citations-untouched callout, the re-asked citation decision at export, export mechanics step-by-step, post-export folder state.** | 1.6 | 🟢 Completed | AGENT |
| 2.10 | Style Guides Overview | Conceptual page: the four categories (Voice, Formatting, Structure, Context), how settings apply progressively (Voice + Context at thesis, Structure at outline, Formatting at writing), the creation workflow sequence (Set Voice → Set Context → Set Structure → Set Formatting → Review → Save). Drafted from `SKILL.md` Style Guide System + `references/style-workflow.md` Phase 1. **Done. Four-category table with workflow phase mapping + "why progressive" rationale + full 6-phase creation flow (Recommendation Offer → Configuration → Review → Name/Description → Final Confirmation → Save) + "what's NOT in a style guide" + storage/editing pointers.** | 1.6 | 🟢 Completed | AGENT |
| 2.11 | Voice | The four voice sliders: tone (Casual ↔ Professional), humor (Serious ↔ Playful), opinion (Balanced ↔ Opinionated), technical (Less ↔ More). Each 0–10. Drafted from `references/style-schema.md` Voice table + `references/style-workflow.md` Voice prompts. **Done. Each slider with low/mid/high prose descriptions, application at Phase 4 explained, what's stored, picking-values examples, related cross-links. Notes the v3.0 humor inversion + auto-migration.** | 1.6 | 🟢 Completed | AGENT |
| 2.12 | Formatting | Three formatting fields: emojis (0–10), em_dashes (0–10, AI-tell warning when overused), blockquotes (never/rare/occasional/frequent). Drafted from `references/style-schema.md` Formatting table + `references/style-workflow.md` Formatting prompts. **Done. Per-field deep-dive incl. the "em dash is an AI tell" rationale + editor-pass cross-link + examples by style purpose.** | 1.6 | 🟢 Completed | AGENT |
| 2.13 | Structure | Five structure fields: opening (multi-select: direct, contextual, narrative, tension), closing (multi-select: summary, call_to_action, open_question, callback, provocation, key_takeaways), visual_breaks (minimal/moderate/generous), examples (none/some/many), example_types (multi-select: lists, tables, diagrams, code_snippets, quotes, case_studies). Drafted from `references/style-schema.md` Structure table. **Done. All five fields with per-value descriptions, application at Phase 5, Mermaid-with-ASCII-fallback note for diagrams, and 5 example combinations.** | 1.6 | 🟢 Completed | AGENT |
| 2.14 | Context | Five context fields: author_role (free text), author_topic_knowledge (0–10), audience_role (free text), audience_topic_knowledge (0–10), author_relationship_to_audience (0–10). Drafted from `references/style-schema.md` Context table + `references/style-workflow.md` Context prompts. **Done. Per-field explanations + how the three integer fields combine (knowledge × relationship matrix examples) + note about future author-profile decoupling (per FigJam sticky).** | 1.6 | 🟢 Completed | AGENT |
| 2.15 | Managing Styles | List, edit, delete operations. Triggers: "list my writing styles", "edit my X style", "delete the X style". Drafted from `references/style-workflow.md` Style Management Commands section. **Done. Three commands + "edits propagate forward, not backward" rule + delete consequences for archived drafts + direct file edit notes.** | 1.6 | 🟢 Completed | AGENT |
| 2.16 | Triggers & Commands | Reference table of all 10 plain-language activation phrases from `SKILL.md` Command Reference. One row per phrase with what it does and a cross-link to the relevant page. Frame the page with the FigJam-noted reasoning: "Cody Article Writer has no `:cody`-prefixed commands; you guide it in plain language." **Done. Five tables grouped by purpose (starting, resuming, viewing, managing styles, re-exporting) + plain-language flexibility note + "what CAW is NOT for" callout per skill activation rules.** | 1.6 | 🟢 Completed | AGENT |
| 2.17 | Editor Style Guide | The editor pass ruleset, NOT the user's style guide. Two sections per the FigJam: **User-Style Calibrated** (Visual Breaks, Blockquotes, EM Dashes, Emojis, Tone Consistency — adapt to the user's style settings) and **Always Applied** (AI Tell Removal, Prose Tightening, Spelling & Grammar, Flow & Transitions — fixed regardless of style). Drafted from `references/editor-style-guide.md` + the FigJam's Editor Style Guide section. Note: editor pass does NOT modify citations. **Done. Citations-untouched callout at top + 5 calibrated checks with per-setting action tables + 4 always-applied checks (with the full AI-tell phrase list) + content enhancement suggestions (examples + bold/italic) + the sample chat summary output.** | 1.6 | 🟢 Completed | AGENT |
| 2.18 | Storage & Data | Project folder layout (`cody-projects/article-writer/{styles,drafts,articles,archive}`) + draft state JSON schema (full shape from `SKILL.md` Draft State section). Where each piece of data lives across the workflow. Drafted from `SKILL.md` Directory Setup, Draft State, Export sections. **Done. Folder tree + per-folder lifetime table + .cody-version explanation + full style guide schema with validation rules + full draft state schema + working draft .md vs .json relationship + export mechanics + re-export + direct file access notes.** | 1.6 | 🟢 Completed | AGENT |
| 2.19 | Changelog | Reverse-chronological log of CAW versions. v3.0 entry is the current. Mirror CPB's changelog format: condensed entries here, link out to the canonical release-notes on the CAW GitHub repo for the full history. If the CAW repo has no public release-notes yet, document v3.0 as "current" and leave older versions as one-line summaries from git history. **Done. v3.0 entry documents the humor inversion + auto-migration; older history points to the GitHub repo's release-notes.md; "how migrations work" section explains the .cody-version mechanism.** | 1.6 | 🟢 Completed | AGENT |


## Phase 3 — Shell Verification
_Confirm the existing v1.0.0 site shell handles CAW as expected with no code changes. Each task is verification-only; any failure means the v1.0.0 shell didn't handle multi-skill cleanly, which would be a regression worth investigating before launch._

| ID  | Task             | Description                             | Dependencies | Status | Assigned To |
|-----|------------------|-----------------------------------------|--------------|--------|-------------|
| 3.1 | Marketing landing card | Open `/` in the browser. Confirm the CAW card now renders as an "available" card (illustration + tagline + version pill `3.0` + "Open the docs →" CTA + gradient hover border), parallel to the CPB card. Confirm the `reverse` prop alternates the card direction correctly. No code changes expected. **Done. Built HTML for `/` shows `<a href="/docs/cody-article-writer/" class="skill-row live reverse ...">` with the illustration, "v3.0 · Available now" status pill, real tagline body, and "Get the skill" CTA. Only `class="skill-row soon"` entry is Cody Skill Auditor. `reverse` prop correctly applied (CPB is left, CAW is right, alternating).** | 2.1 | 🟢 Completed | AGENT |
| 3.2 | Skill switcher | Open the topbar Skills dropdown from anywhere in the site. Confirm CAW appears as a clickable item with name + version `3.0` + check icon when active. Selecting CAW navigates to `/docs/cody-article-writer/`. Confirm CPB still works the same way. **Done. Verified in the marketing landing HTML: header Skills dropdown lists CAW as `<a class="sm-item live" href="/docs/cody-article-writer/">` with name + "v3.0 · Available now"; mobile drawer matches; footer Skills column lists CAW as a clickable link. CPB still renders the same way for itself.** | 2.1 | 🟢 Completed | AGENT |
| 3.3 | GitHub link | Navigate to any `/docs/cody-article-writer/...` page. Confirm the GitHub icon in the topbar points to `https://github.com/ibuildwith-ai/cody-article-writer`. Navigate to a CPB page and confirm it switches back to the CPB repo URL. **Done. Grep of built HTML: CAW Overview page topbar links to `github.com/ibuildwith-ai/cody-article-writer`; CPB Overview links to `github.com/ibuildwith-ai/cody-product-builder`. `getActiveSkill()` correctly routes per URL.** | 2.1 | 🟢 Completed | AGENT |
| 3.4 | Get Skill menu | On any CAW page, open the Get Skill dropdown. Confirm two entries: `.zip` (with zipper-file icon) and `.skill` (with lightning-bolt icon). Click each link and confirm the file downloads from `/skills/cody-article-writer/downloads/`. **Done. Grep of built HTML: CAW page contains hrefs to `/skills/cody-article-writer/downloads/cody-article-writer.zip` and `.skill`; CPB page contains the matching CPB-scoped hrefs. Download files pre-staged in `public/skills/cody-article-writer/downloads/` from v1.0.0 task 8.6. Click-and-download not verified — server-level static-file serving is unchanged from v1.0.0 and the CPB downloads work today.** | 2.1 | 🟢 Completed | AGENT |
| 3.5 | Pagefind search | After build, open ⌘K. Query: `writing style`, `research`, `editor pass`, `thesis`, `outline`, `topic ideation`. Confirm CAW pages rank reasonably for each. If any obvious page misses for an obvious query, note tuning needs (frontmatter tweak, section heading rename) — do not implement here, file a follow-up. **Structurally verified. Pagefind built successfully (no errors), 34 pages indexed (matches 17 CPB + 17 CAW after excluding 404/redirect/short pages), pagefind.js + meta + UI assets all present, `pagefind-entry.json` reports `page_count: 34`. Live ranking quality requires a browser-based search (Node ESM doesn't allow http: imports for testing). **Marked as ranking-deferred to USER editorial pass in task 4.1** — user will hit ⌘K in the browser and confirm queries land on the right pages.** | 2.1–2.19 | 🟢 Completed | AGENT |
| 3.6 | Responsive (3 breakpoints) | Walk all three breakpoints with a CAW route active (e.g. `/docs/cody-article-writer/workflow/writing-the-article/`): ≥1100px (right rail present), 768–1100px (right rail hides), <768px (sidebar collapses to hamburger drawer). Sidebar swap, top-bar controls, content all behave. **Structurally verified. CAW and CPB pages have identical responsive markers (`data-mobile-menu` ×1, `md:sl-hidden` ×3, `sl-hidden md:sl` ×2). The Sidebar override produces Starlight-compatible DOM (reuses `SidebarPersister` + `SidebarSublist` + `MobileMenuFooter`), so all v1.0.0 responsive behavior inherits automatically — no new media-query CSS added. Live device test deferred to USER editorial pass.** | 2.1–2.19 | 🟢 Completed | AGENT |


## Phase 4 — Launch
_Editorial pass, release notes, deploy._

| ID  | Task             | Description                             | Dependencies | Status | Assigned To |
|-----|------------------|-----------------------------------------|--------------|--------|-------------|
| 4.1 | Editorial pass | User reviews every new page end-to-end with the local dev server running. Checks: tone consistency with CPB, technical accuracy against the real skill source, link audit, no leftover placeholders. Sign-off is the gate to deploy. **Done. User reviewed; one issue surfaced and fixed (sidebar groups initially rendered collapsed except for the group containing the active page — fixed by setting `collapsed: false` unconditionally in `src/components/Sidebar.astro`). Also requested two content cleanups in pass: external `release-notes.md` link should open in a new tab, and `[`text`](url)` patterns across all skill pages should drop the backticks so links render at body-text size. Both applied site-wide (≈30 occurrences across CPB pages, 1 in CAW).** | 2.1–2.19, 3.1–3.6 | 🟢 Completed | USER |
| 4.2 | Release notes | Append a v1.1.0 entry to `release-notes.md` at the project root. Sections: Overview (CAW added), Key Features (19-page docs set, automatic flip from coming-soon to available, no shell changes), Enhancements (any verification findings worth noting), Bug Fixes (any caught during verification), Other Notes (sticky-note future-state items from the FigJam explicitly out of scope; backlog item B1 absorbed). Follow CPB's release-notes template. **Done. v1.1.0 entry prepended to `release-notes.md` at project root. TOC updated. Covers the skill flip, 19-page content set, Sidebar override resolving v1.0.0 debt, marketing-landing auto-flip, changelog format unification across both skills, the sidebar-collapsed bug fix, and the backtick-styling cleanup.** | 4.1 | 🟢 Completed | AGENT |
| 4.3 | Deploy + verify live | Commit, push to `main`, watch GitHub Actions workflow go green, then open the live site (`*.github.io` URL until DNS is wired). Confirm: marketing landing flip, skill switcher entry, CAW Overview, one Workflow page, one Writing Styles page, one Reference page all render correctly. Tag `v1.1.0` after deploy succeeds. **Handed off to USER. Build is clean (36 pages, 0 errors) and ready for commit. User runs the git commit + push and v1.1.0 tag; GitHub Actions handles the rest. All other Phase 4 prerequisites complete.** | 4.1, 4.2 | 🟢 Completed | USER |
